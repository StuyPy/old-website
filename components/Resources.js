import meetings from '../data/meetings/meetings.json';

/*
use cards to add the meetings
*/

export default function Resources() {

  return (
    <div className="container my-5">
    <h1>Resources</h1>
    add some code here to display meetings from meetings.json
    </div>
  );
}
